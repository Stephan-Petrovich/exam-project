import type { ReactElement } from "react";

const ImageLoaderPage = (): ReactElement => {
    return (
        <div className="image-loader-page-block">
            Hello! It's IMAGELOADERPAGE
        </div>
    );
};

export default ImageLoaderPage;
