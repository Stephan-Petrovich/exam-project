import type { ReactElement } from "react";

const SenderDataPage = (): ReactElement => {
    return (
        <div className="sender-data-page-block">Hello! It's SENDERDATAPAGE</div>
    );
};

export default SenderDataPage;
